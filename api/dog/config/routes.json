{
  "routes": [
    {
      "method": "GET",
      "path": "/dogs",
      "handler": "Dog.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/dogs/count",
      "handler": "Dog.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/dogs/:id",
      "handler": "Dog.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/dogs",
      "handler": "Dog.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/dogs/:id",
      "handler": "Dog.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/dogs/:id",
      "handler": "Dog.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
